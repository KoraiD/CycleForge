{
  "name": "cycleforge",
  "private": true,
  "version": "0.1.0",
  "description": "CycleForge — visual cycling training planner (ClickHouse × Trigger.dev hackathon)",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "dev:trigger": "npm run dev:trigger --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "typecheck": "npm run typecheck --prefix apps/web",
    "test": "npm run test --prefix apps/web",
    "check": "npm run check --prefix apps/web",
    "seed:clickhouse": "npm run seed:clickhouse --prefix apps/web"
  },
  "license": "MIT"
}
